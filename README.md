
# Ecommerce App

A modern ecommerce platform built with React, Vite, TypeScript, and Tailwind CSS. Features product browsing, shopping cart, wishlist, and Stripe payment integration.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
  